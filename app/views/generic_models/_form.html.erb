<%= form_with(model: generic_model, local: true) do |form| %>
  <% if generic_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(generic_model.errors.count, "error") %> prohibited this generic_model from being saved:</h2>

      <ul>
      <% generic_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
